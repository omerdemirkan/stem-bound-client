import AppLayout from "../../components/AppLayout";

const Courses: React.FC = () => {
    return (
        <AppLayout>
            <h4>courses</h4>
        </AppLayout>
    );
};

export default Courses;
