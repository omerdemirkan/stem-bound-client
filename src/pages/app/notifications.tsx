import AppLayout from "../../components/AppLayout";

const Notifications: React.FC = () => {
    return (
        <AppLayout>
            <h4>notifications</h4>
        </AppLayout>
    );
};

export default Notifications;
