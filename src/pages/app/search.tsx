import AppLayout from "../../components/AppLayout";

const Search: React.FC = () => {
    return (
        <AppLayout>
            <h4>search</h4>
        </AppLayout>
    );
};

export default Search;
