import AppLayout from "../../components/AppLayout";

const Messaging: React.FC = () => {
    return (
        <AppLayout>
            <h4>messaging</h4>
        </AppLayout>
    );
};

export default Messaging;
