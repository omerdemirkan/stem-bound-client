import AppLayout from "../../components/AppLayout";

const Schedule: React.FC = () => {
    return (
        <AppLayout>
            <h4>schedule</h4>
        </AppLayout>
    );
};

export default Schedule;
