import AppLayout from "../../components/AppLayout";

const Dashboard: React.FC = () => {
    return (
        <AppLayout>
            <h4>dashboard</h4>
        </AppLayout>
    );
};

export default Dashboard;
